#
# Dependencies Required to Build Books or Websites
#

# Basically, we only need the `texgit` Python companion package.
# You can find it at https://thomasweise.github.io/texgit_py,
# https://github.com/thomasWeise/texgit_py, or at
# https://pypi.org/project/texgit.
texgit == 0.9.11

# minify_html is needed to minify html output.
minify_html == 0.18.1

# for converting the additional markdown files to HTML
markdown == 3.10.2

# for converting files to HTML
Pygments == 2.19.2
